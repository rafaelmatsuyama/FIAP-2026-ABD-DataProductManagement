version: 2

sources:
  - name: raw_sources
    schema: main
    description: "Raw ingested source tables in analytics.duckdb."
    tables:
      - name: transactions
        description: "Raw financial transaction events."
      - name: customers
        description: "Raw registered customer records."
