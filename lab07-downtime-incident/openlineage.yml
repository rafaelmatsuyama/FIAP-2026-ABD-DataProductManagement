# OpenLineage SDK Transport Configuration for Lab 07
# Emits operational telemetry events directly to Marquez API (port 5000)
transport:
  type: http
  url: http://localhost:5000
  endpoint: api/v1/lineage
