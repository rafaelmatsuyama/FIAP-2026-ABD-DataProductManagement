{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "product_name": "financial_transactions_product",
  "version": "1.0.0",
  "domain": "Fintech & Core Banking",
  "fields": [
    {
      "name": "transaction_id",
      "type": "VARCHAR",
      "required": true,
      "unique": true,
      "description": "Unique identifier for the financial transaction."
    },
    {
      "name": "customer_id",
      "type": "VARCHAR",
      "required": true,
      "unique": false,
      "description": "Pseudonymized identifier of the paying customer."
    },
    {
      "name": "amount",
      "type": "DECIMAL(18,2)",
      "required": true,
      "minimum": 0.01,
      "description": "Monetary value of the transaction."
    },
    {
      "name": "payment_method",
      "type": "VARCHAR",
      "required": true,
      "allowed_values": ["PIX", "CREDIT_CARD", "DEBIT_CARD", "BOLETO"],
      "description": "Payment method or settlement rail used."
    },
    {
      "name": "status",
      "type": "VARCHAR",
      "required": true,
      "allowed_values": ["COMPLETED", "PENDING", "FAILED"],
      "description": "Operational processing status of the transaction."
    },
    {
      "name": "fee",
      "type": "DECIMAL(18,2)",
      "required": true,
      "minimum": 0.0,
      "description": "Settlement fee charged on the transaction."
    },
    {
      "name": "currency",
      "type": "VARCHAR",
      "required": true,
      "allowed_values": ["BRL", "USD", "EUR"],
      "description": "Transaction currency code (ISO 4217)."
    },
    {
      "name": "created_at",
      "type": "TIMESTAMP",
      "required": true,
      "description": "Timestamp when the transaction record was created."
    }
  ]
}
